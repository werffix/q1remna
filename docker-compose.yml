services:
  3xui-shopbot:
    build: .
    restart: unless-stopped
    environment:
      - TZ=Europe/Moscow
    ports:
      - '1488:1488'
    volumes:
      - .:/app/project
    container_name: 3xui-shopbot
